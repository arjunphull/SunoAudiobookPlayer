<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.arjunphull.sunoaudiobookplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SunoAudiobookPlayer">
        <activity android:name=".ui.LibraryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.PlayerActivity"
            android:parentActivityName=".ui.LibraryActivity"
            android:launchMode="singleTask">
        </activity>
        <service
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"
            android:name=".audio.PlayerService">
        </service>
    </application>
</manifest>